
  {{#if adc}}

  
    <div class="alert alert-success hidden" role="alert" id="minhadiv"style="margin-left: 70%;
    position: absolute;
    margin-top: -17px;
    height: 47px;">
  <button type="button" class="close" data-dismiss="alert" aria-label="Close" style="margin-top: -15px; margin-right: -8px"><span aria-hidden="true">&times;</span></button>
    Adicionado ao carrinho
</div>
 
        {{/if}} 
 {{#each produto}}
  
 <div class="row">
   
   {{#each this }}
      <div class="col-sm-6 col-md-4" style="padding: 35px">
        <div class="thumbanail">
          <img src="{{this.img}}" class="img-responsive" alt="..." style="  max-width: 100%;
  height: 300px;" />
            <div class="caption">
                <h5>{{this.title}}</h5>
                    <div class="clearfix">
                       <div class="price pull-left"  style="font-style:italic;">R${{this.price}}</div>                 
                          <a href="/beb/carrinho/{{this._id}}" class="btn btn-primary pull-right"  >Adicionar ao carrinho</a>
                        </div>
                       </div>
        
             </div>
        </div>
  {{/each}}
      </div>

{{/each}}














